import { LaTeXParser } from './src/lib/latexParser'; import * as fs from 'fs'; const parser = new LaTeXParser(); const tex = fs.readFileSync('D:/StrangerThings Season 5/JEE-11.5-P- work,enegry and  power/JEE-11-P-work,energyandpower.tex', 'utf8'); const result = parser.parse(tex); console.log('Total questions:', result.allQuestions.length); let ms = 0; result.allQuestions.forEach(q => { if (q.options.length === 0) ms++; }); console.log('Without options:', ms); console.log('Warnings:', result.warnings); const q = result.allQuestions.find(q => q.text.includes('There block A, B and C are lying')); console.log('Fixed block question:', JSON.stringify(q, null, 2)); 
